# Document Intelligence Refinery

A scalable, production-ready document intelligence system.

## Setup

1.  Requires Python 3.11+.
2.  Install dependencies: `pip install .`
3.  Run development tools: `ruff check .` or `mypy src`
